# Allow @biomejs/biome and esbuild to run their post-install scripts.
# These packages download platform-specific native binaries (not arbitrary code).
# Required for pnpm v10+ which blocks build scripts by default.
onlyBuiltDependencies[]=@biomejs/biome
onlyBuiltDependencies[]=esbuild
