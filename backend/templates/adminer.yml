x-hosty:
  name: "Adminer"
  version: 1
  category: "Developer Tools"
  icon: "database"
  description: "Deploy Adminer for browser-based database administration."
  web: web
  ports:
    - web

services:
  web:
    image: docker.io/library/adminer:4
    ports:
      - 8080:8080
