"""v2 domain core (ADR-013): pure specs, validation grammar, and the
planner. NOTHING in this package performs I/O or imports a module that
does — that property is what makes the hardest logic in the system the
most testable."""
